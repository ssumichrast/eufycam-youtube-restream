# Template only -- do NOT commit a filled-in copy of this file. It holds the
# camera password and the YouTube stream key in plaintext.
#
# Prefer generating it straight from your .env instead of editing this:
#
#   kubectl create secret generic eufycam-youtube-restream --from-env-file=.env
#
# If you do fill this in locally, save it as secret.yaml -- .gitignore
# already excludes that name.
apiVersion: v1
kind: Secret
metadata:
  name: eufycam-youtube-restream
type: Opaque
stringData:
  CAMERA_RTSP_ADDRESS: "192.168.1.50:554/live0"
  CAMERA_USERNAME: "admin"
  CAMERA_PASSWORD: "changeme"
  YOUTUBE_INGEST_URL: "rtmp://a.rtmp.youtube.com/live2"
  YOUTUBE_STREAM_KEY: "xxxx-xxxx-xxxx-xxxx-xxxx"
